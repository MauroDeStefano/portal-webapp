export type TMostClickedLocation = {
    id: number,
    title: string,
    description: string,
    images: {
        alt: string,
        mobile: {
            src: string
        },
        desktop: {
            src: string
        }
    }
}
