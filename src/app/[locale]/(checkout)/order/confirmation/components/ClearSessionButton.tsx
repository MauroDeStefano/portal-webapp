'use client';

import React from "react";

type TClearSessionButtonProps = {
    children: React.ReactNode
}

export default function ClearSessionButton({children}: TClearSessionButtonProps) {
    return (
        <>

        </>
    );
}